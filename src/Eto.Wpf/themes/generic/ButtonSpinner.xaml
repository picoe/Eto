<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:efc="clr-namespace:Eto.Wpf.Forms.Controls"
	xmlns:s="clr-namespace:System;assembly=mscorlib">

	<!--
		The ButtonSpinner ControlTemplate is defined ONCE here (ButtonSpinnerTemplate) and shared by every
		theme. Themes only override the resource keys it binds to:
		SpinnerButtonColumns        - 1 => buttons stacked vertically, 2 => side by side (fluent)
		SpinnerButtonAreaWidth      - width of the spin-button area (NaN = auto)
		SpinnerCornerRadius         - corner radius of the outer chrome
		SpinnerRepeatButtonStyle    - style for the increase/decrease RepeatButtons
		SpinnerIncreaseGlyph / SpinnerDecreaseGlyph                 - normal glyphs
		SpinnerIncreaseGlyphDisabled / SpinnerDecreaseGlyphDisabled - disabled glyphs
		Hover/focus/disabled chrome colours are theme specific and live in each theme's Style.Triggers
		(they set the control's Background/BorderBrush, which the template binds to) rather than in the
		shared template, so a theme never inherits another theme's state colours.
	-->

	<Geometry x:Key="UpArrowGeometry">M0,5 L4.5,.5 9,5 6,5 4.5,3.5 3,5 z</Geometry>
	<Geometry x:Key="DownArrowGeometry">M0,0 L3,0 4.5,1.5 6,0 9,0 4.5,4.5 z</Geometry>

	<BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

	<!--  Vertical buttons, system colours: the default ("none"/classic) look  -->
	<s:Int32 x:Key="SpinnerButtonColumns">1</s:Int32>
	<s:Double x:Key="SpinnerButtonAreaWidth">17</s:Double>
	<CornerRadius x:Key="SpinnerCornerRadius">0</CornerRadius>

	<DataTemplate x:Key="SpinnerIncreaseGlyph">
		<Path
			Width="9"
			Height="5"
			HorizontalAlignment="Center"
			VerticalAlignment="Center"
			Data="{StaticResource UpArrowGeometry}"
			Fill="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
			Focusable="False"
			SnapsToDevicePixels="True" />
	</DataTemplate>

	<DataTemplate x:Key="SpinnerDecreaseGlyph">
		<Path
			Width="9"
			Height="5"
			HorizontalAlignment="Center"
			VerticalAlignment="Center"
			Data="{StaticResource DownArrowGeometry}"
			Fill="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
			Focusable="False"
			SnapsToDevicePixels="True" />
	</DataTemplate>

	<DataTemplate x:Key="SpinnerIncreaseGlyphDisabled">
		<Path
			Width="9"
			Height="5"
			HorizontalAlignment="Center"
			VerticalAlignment="Center"
			Data="{StaticResource UpArrowGeometry}"
			Fill="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
			Focusable="False"
			SnapsToDevicePixels="True" />
	</DataTemplate>

	<DataTemplate x:Key="SpinnerDecreaseGlyphDisabled">
		<Path
			Width="9"
			Height="5"
			HorizontalAlignment="Center"
			VerticalAlignment="Center"
			Data="{StaticResource DownArrowGeometry}"
			Fill="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
			Focusable="False"
			SnapsToDevicePixels="True" />
	</DataTemplate>

	<Style x:Key="SpinnerRepeatButtonStyle" TargetType="{x:Type RepeatButton}">
		<Setter Property="Background" Value="Transparent" />
		<Setter Property="BorderThickness" Value="0" />
		<Setter Property="FocusVisualStyle" Value="{x:Null}" />
		<Setter Property="HorizontalAlignment" Value="Stretch" />
		<Setter Property="VerticalAlignment" Value="Stretch" />
		<Setter Property="Height" Value="9" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type RepeatButton}">
					<Border
						x:Name="ButtonBorder"
						HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
						VerticalAlignment="{TemplateBinding VerticalAlignment}"
						Background="{TemplateBinding Background}"
						SnapsToDevicePixels="True">
						<ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
					</Border>
					<ControlTemplate.Triggers>
						<Trigger Property="IsMouseOver" Value="True">
							<Setter TargetName="ButtonBorder" Property="Background" Value="#14000000" />
						</Trigger>
						<Trigger Property="IsPressed" Value="True">
							<Setter TargetName="ButtonBorder" Property="Background" Value="#22000000" />
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<!--  The shared template. Every theme uses this; only the keys above change.  -->
	<ControlTemplate x:Key="ButtonSpinnerTemplate" TargetType="{x:Type efc:EtoButtonSpinner}">
		<Border
			x:Name="RootBorder"
			Background="{TemplateBinding Background}"
			BorderBrush="{TemplateBinding BorderBrush}"
			BorderThickness="{TemplateBinding BorderThickness}"
			CornerRadius="{DynamicResource SpinnerCornerRadius}"
			SnapsToDevicePixels="True">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition x:Name="firstContentColumn" Width="*" />
					<ColumnDefinition x:Name="secondContentColumn" Width="Auto" />
				</Grid.ColumnDefinitions>

				<ContentPresenter
					x:Name="contentPresenter"
					Margin="{TemplateBinding Padding}"
					HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
					VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
					Content="{TemplateBinding EditContent}"
					Focusable="False"
					SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
					Visibility="{TemplateBinding ShowContentArea,
					                             Converter={StaticResource BooleanToVisibilityConverter}}">
                    <ContentPresenter.Resources>
                        <SolidColorBrush x:Key="TextControlBackgroundPointerOver" Color="Transparent" />
                        <SolidColorBrush x:Key="TextControlBackgroundDisabled" Color="Transparent" />
                        <SolidColorBrush x:Key="TextControlBackground" Color="Transparent" />
                    </ContentPresenter.Resources>
                </ContentPresenter>

				<UniformGrid
					x:Name="gridContent"
					Grid.Column="1"
					Width="{DynamicResource SpinnerButtonAreaWidth}"
					Columns="{DynamicResource SpinnerButtonColumns}"
					Visibility="{TemplateBinding ShowButtonSpinner,
					                             Converter={StaticResource BooleanToVisibilityConverter}}">

					<RepeatButton
						x:Name="PART_IncreaseButton"
						ContentTemplate="{DynamicResource SpinnerIncreaseGlyph}"
						IsTabStop="{TemplateBinding IsTabStop}"
						Style="{DynamicResource SpinnerRepeatButtonStyle}" />

					<RepeatButton
						x:Name="PART_DecreaseButton"
						ContentTemplate="{DynamicResource SpinnerDecreaseGlyph}"
						IsTabStop="{TemplateBinding IsTabStop}"
						Style="{DynamicResource SpinnerRepeatButtonStyle}" />
				</UniformGrid>
			</Grid>
		</Border>
		<ControlTemplate.Triggers>
			<Trigger SourceName="PART_IncreaseButton" Property="IsEnabled" Value="False">
				<Setter TargetName="PART_IncreaseButton" Property="ContentTemplate" Value="{DynamicResource SpinnerIncreaseGlyphDisabled}" />
			</Trigger>
			<Trigger SourceName="PART_DecreaseButton" Property="IsEnabled" Value="False">
				<Setter TargetName="PART_DecreaseButton" Property="ContentTemplate" Value="{DynamicResource SpinnerDecreaseGlyphDisabled}" />
			</Trigger>
			<Trigger Property="ButtonSpinnerLocation" Value="Left">
				<Setter TargetName="firstContentColumn" Property="Width" Value="Auto" />
				<Setter TargetName="secondContentColumn" Property="Width" Value="*" />
				<Setter TargetName="contentPresenter" Property="Grid.Column" Value="1" />
				<Setter TargetName="gridContent" Property="Grid.Column" Value="0" />
			</Trigger>
			<Trigger Property="ShowContentArea" Value="False">
				<Setter TargetName="firstContentColumn" Property="Width" Value="0" />
				<Setter TargetName="secondContentColumn" Property="Width" Value="*" />
			</Trigger>
		</ControlTemplate.Triggers>
	</ControlTemplate>

	<Style TargetType="{x:Type efc:EtoButtonSpinner}">
		<Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.WindowBrushKey}}" />
		<Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}" />
		<Setter Property="BorderThickness" Value="1" />
		<Setter Property="HorizontalContentAlignment" Value="Stretch" />
		<Setter Property="VerticalContentAlignment" Value="Stretch" />
		<Setter Property="Padding" Value="2,2" />
		<Setter Property="Template" Value="{StaticResource ButtonSpinnerTemplate}" />
		<Style.Triggers>
			<Trigger Property="IsMouseOver" Value="True">
				<Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}" />
			</Trigger>
			<Trigger Property="IsKeyboardFocusWithin" Value="True">
				<Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}" />
			</Trigger>
			<Trigger Property="IsEnabled" Value="False">
				<Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" />
			</Trigger>
		</Style.Triggers>
	</Style>

</ResourceDictionary>
