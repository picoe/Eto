<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:s="clr-namespace:System;assembly=mscorlib"
                    xmlns:efc="clr-namespace:Eto.Wpf.Forms.Controls">

    <!--
        Fluent only overrides the resource keys the shared ButtonSpinnerTemplate (themes/generic/ButtonSpinner.xaml)
        binds to, plus a style that supplies the Fluent chrome colours. The ControlTemplate itself is NOT redefined.
    -->

    <!-- Buttons side by side (horizontal) with Fluent rounding -->
    <s:Int32 x:Key="SpinnerButtonColumns">2</s:Int32>
    <s:Double x:Key="SpinnerButtonAreaWidth">NaN</s:Double>
    <CornerRadius x:Key="SpinnerCornerRadius">4</CornerRadius>

    <Geometry x:Key="FluentIncreaseChevron">M1,3 L4.5,0 8,3</Geometry>
    <Geometry x:Key="FluentDecreaseChevron">M1,0 L4.5,3 8,0</Geometry>

    <DataTemplate x:Key="SpinnerIncreaseGlyph">
        <Path Width="9"
              Height="4"
              Data="{StaticResource FluentIncreaseChevron}"
              Stroke="{DynamicResource TextControlButtonForeground}"
              StrokeThickness="1.5"
              StrokeStartLineCap="Round"
              StrokeEndLineCap="Round"
              SnapsToDevicePixels="True"
              HorizontalAlignment="Center"
              VerticalAlignment="Center"
              Focusable="False" />
    </DataTemplate>

    <DataTemplate x:Key="SpinnerDecreaseGlyph">
        <Path Width="9"
              Height="4"
              Data="{StaticResource FluentDecreaseChevron}"
              Stroke="{DynamicResource TextControlButtonForeground}"
              StrokeThickness="1.5"
              StrokeStartLineCap="Round"
              StrokeEndLineCap="Round"
              SnapsToDevicePixels="True"
              HorizontalAlignment="Center"
              VerticalAlignment="Center"
              Focusable="False" />
    </DataTemplate>

    <DataTemplate x:Key="SpinnerIncreaseGlyphDisabled">
        <Path Width="9"
              Height="4"
              Data="{StaticResource FluentIncreaseChevron}"
              Stroke="{DynamicResource TextControlForegroundDisabled}"
              StrokeThickness="1.5"
              StrokeStartLineCap="Round"
              StrokeEndLineCap="Round"
              SnapsToDevicePixels="True"
              HorizontalAlignment="Center"
              VerticalAlignment="Center"
              Focusable="False" />
    </DataTemplate>

    <DataTemplate x:Key="SpinnerDecreaseGlyphDisabled">
        <Path Width="9"
              Height="4"
              Data="{StaticResource FluentDecreaseChevron}"
              Stroke="{DynamicResource TextControlForegroundDisabled}"
              StrokeThickness="1.5"
              StrokeStartLineCap="Round"
              StrokeEndLineCap="Round"
              SnapsToDevicePixels="True"
              HorizontalAlignment="Center"
              VerticalAlignment="Center"
              Focusable="False" />
    </DataTemplate>

    <Style x:Key="SpinnerRepeatButtonStyle" TargetType="{x:Type RepeatButton}" BasedOn="{StaticResource {x:Type RepeatButton}}">
        <Setter Property="Background" Value="Transparent" />
		<Setter Property="HorizontalAlignment" Value="Stretch" />
		<Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Margin" Value="2" />
        <Setter Property="Padding" Value="2,4" />
		<Setter Property="MinHeight" Value="20" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <Border x:Name="ButtonBorder"
                            Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}"
							HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
							VerticalAlignment="{TemplateBinding VerticalAlignment}"
							CornerRadius="2"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource ControlAltFillColorTertiaryBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource ControlAltFillColorQuarternaryBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="Transparent" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Reuse the shared template; supply Fluent chrome colours via setters + state triggers. -->
    <Style TargetType="{x:Type efc:EtoButtonSpinner}">
        <Setter Property="Template" Value="{DynamicResource ButtonSpinnerTemplate}" />
        <Setter Property="Foreground" Value="{DynamicResource TextControlForeground}" />
        <Setter Property="Background" Value="{DynamicResource TextControlBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource TextControlElevationBorderBrush}" />
        <Setter Property="BorderThickness" Value="{DynamicResource TextControlBorderThemeThickness}" />
        <Setter Property="Padding" Value="4,2" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Style.Triggers>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter Property="Background" Value="{DynamicResource TextControlBackgroundFocused}" />
                <Setter Property="BorderBrush" Value="{DynamicResource TextControlBorderBrushFocused}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="{DynamicResource TextControlBackgroundDisabled}" />
                <Setter Property="BorderBrush" Value="{DynamicResource TextControlBorderBrushDisabled}" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsEnabled" Value="True" />
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="IsKeyboardFocusWithin" Value="False" />
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource TextControlBackgroundPointerOver}" />
            </MultiTrigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
